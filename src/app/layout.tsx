import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AI Music Studio - Learn, Create, and Earn with Music",
  description: "Transform your voice into AI-enhanced music. Learn singing with AI feedback, create professional tracks with ACE-Step technology, and sell your music in our integrated marketplace.",
  keywords: ["AI Music", "Singing Lessons", "Music Creation", "ACE-Step", "Music Marketplace", "Vocal Training"],
  authors: [{ name: "AI Music Studio Team" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}